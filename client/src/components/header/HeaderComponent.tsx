import NavbarComponent from "./NavbarComponent";

const HeaderComponent = () => {
    return (
        <header>
            <NavbarComponent />
        </header>
    );
}

export default HeaderComponent;